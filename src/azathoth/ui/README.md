## Legacy implementation

The existing implementation is being preserved during the MVP redesign.

New development is taking place in the domain-oriented packages under `azathoth/`.

The legacy code remains available for reference until equivalent capabilities have been implemented and verified.